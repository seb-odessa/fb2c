<!DOCTYPE html>
<html lang="ru">

<head>
    <title>{{last_name}} {{first_name}} {{middle_name}}</title>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
</head>

<body>

    <h3>{{last_name}} {{first_name}} {{middle_name}}</h3>

    <ul>
        {{#each titles}}
            <li>{{{this}}}</li>
        {{/each}}
    </ul>

    <a href="/">домой</a>

</body>
</html>